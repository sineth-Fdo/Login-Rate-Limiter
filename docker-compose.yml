services:
  redis:
    image: redis:latest
    container_name: ratelimiter-redis
    ports:
      - "6379:6379"

  app:
    build: .
    container_name: ratelimiter-app
    ports:
      - "8080:8080"
    environment:
      - REDIS_ADDR=redis:6379
    depends_on:
      - redis
